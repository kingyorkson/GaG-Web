{
  "name": "growing-gardening-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cd ../client && npx vite",
    "build": "cd ../client && npx vite build --outDir ../web/dist",
    "preview": "npx serve dist",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "gh-pages": "^6.1.1",
    "serve": "^14.2.3"
  }
}
